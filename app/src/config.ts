
export const backendURL = window.location.hostname == 'localhost' ? 
'http://localhost:3050/api' : '/api'



export const storeKeys = {
    userToken: 'MegatireUserToken',
    userSchema: 'MegatireUser'
}